
				"""elif 257 <= code < 265:
					length = code - 257 + 3
					print(length)

					code = ""
					p = -2 
					hft_dist.resetCurNode()
					while p == -2:
						new_bit = self.readBits(1)
						code += str(new_bit)
						p = hft_dist.nextNode(str(new_bit))
					code = int(hdist_codes.index(code))

					print(code)

					if 0 <= code < 5:
						dist = code + 1
					

					for _ in range(length):
						text.append(text[-dist])"""